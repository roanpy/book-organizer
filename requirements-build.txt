# Packaging toolchain. Desktop runtime dependencies are kept separately so
# normal tests do not install PyInstaller or PyObjC.
-r requirements-desktop.txt

altgraph==0.17.5; sys_platform == "darwin"
macholib==1.16.3; sys_platform == "darwin"
pyinstaller==6.22.0
pyinstaller-hooks-contrib==2026.6
setuptools==83.0.0
