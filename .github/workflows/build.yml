name: Build Multi-Platform

on:
  push:
    tags:
      - 'v*'  # 仅在创建版本标签时触发
  workflow_dispatch:  # 允许手动触发

jobs:
  build-windows:
    runs-on: windows-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-build.txt
          
      - name: Build Windows App
        run: |
          pyinstaller BookOrganizer.spec
            
      - name: Upload Windows Artifact
        uses: actions/upload-artifact@v4
        with:
          name: BookOrganizer-Windows
          path: dist/BookOrganizer/
          retention-days: 30

  build-macos:
    runs-on: macos-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
          
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r requirements-build.txt
          
      - name: Build macOS App
        run: |
          pyinstaller BookOrganizer.spec
            
      - name: Upload macOS Artifact
        uses: actions/upload-artifact@v4
        with:
          name: BookOrganizer-macOS
          path: dist/BookOrganizer.app/
          retention-days: 30
